function inverserTableau(tab) {
    let tab2 = 0;
	for (let i = tab.length - 1; i >= 0; i--) {
      tab2 = tab[i].push
	}
    return tab2
}

console.log(inverserTableau([1, 2, 3]));
inverserTableau(["a", "b", "c"]);
inverserTableau([42]);
